{% extends 'admin/customer/customer.html.twig' %}

{% block title %}Modifier un Client{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {#<link href="{{ asset('admin/css/bootstrap2-toggle.css') }}" rel="stylesheet">#}
{% endblock %}


{% block content %}
	<div class="admin_header">
		<div class="row">
			<div class="col-md-6">
				<div class="row">
					<div class="col-md-6">
						<h2>Modifier un client</h2>
					</div>
					<div class="col-md-6">
						<a href="{{ path('customer_activity_edit', {'id': customer.customeractivity.id}) }}" class="btn btn-primary" style="margin-top: 10px">Mettre à jour l'activité</a>
					</div>
				</div>
			</div>
			<div class="col-md-6 text-right">
				<a href="{{ path('customer_index') }}" class="btn btn-primary"><i class="fa fa-list"></i> &nbsp; Retour à la liste</a>
			</div>
		</div>
	</div>

    {{ include('admin/customer/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <p>&nbsp;</p>

    {# --{ -- include -- ('admin/customer/_delete_form.html.twig') } -- #}
{% endblock %}



{% block final_javascripts %}
    {#<script src="{{ asset('admin/js/bootstrap-toggle.min.js') }}" type="text/javascript"></script>#}
{% endblock %}

